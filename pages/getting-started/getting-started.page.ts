import page_config from "../page_config";
import { GettingStarted } from "./GettingStarted";

export default page_config(GettingStarted, "Getting Started", {
  route: "/start",
  renderToDom: false,
});
